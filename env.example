# ===========================================
# Supabase Auth – Environment variables
# ===========================================
# 1. Copy this file to .env:  cp env.example .env
# 2. Fill in your values from: Supabase Dashboard → Project Settings → API
# ===========================================

# Required – Project URL (e.g. https://xxxxx.supabase.co)
SUPABASE_URL=your-supabase-project-url

# Required – Anonymous/public key (safe for client-side use)
SUPABASE_ANON_KEY=your-supabase-anon-key

# Optional – Service role key (server-only; bypasses RLS). Needed for admin endpoints.
# Get from: Project Settings → API → service_role (secret)
# SUPABASE_SERVICE_ROLE_KEY=

# Optional – Server port (default: 3024)
PORT=3024

# Optional – Base URL for API (used by frontend on deployed sites)
# Set this to your deployed URL (e.g. https://api.fazla.pro or https://fazla.pro/supabase-auth)
# If not set, frontend will use window.location.origin
# BASE_URL=http://localhost:3024
